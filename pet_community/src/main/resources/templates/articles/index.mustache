<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Pet Community | 게시판</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <style>
        body {
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background-color: #f7f8fa;
        }

        .container {
            max-width: 900px;
            margin: 60px auto;
            padding: 0 20px;
        }

        h1 {
            margin-bottom: 20px;
            color: #333;
        }

        .message {
            margin-bottom: 20px;
            padding: 12px 16px;
            background-color: #e8f5e9;
            color: #2e7d32;
            border-radius: 8px;
            font-weight: 500;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background-color: #fff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
        }

        th, td {
            padding: 14px 16px;
            text-align: left;
        }

        th {
            background-color: #f2f3f5;
            color: #555;
            font-size: 14px;
        }

        tr:not(:last-child) td {
            border-bottom: 1px solid #eee;
        }

        td a {
            color: #ff8c42;
            text-decoration: none;
            font-weight: 500;
        }

        td a:hover {
            text-decoration: underline;
        }

        .empty {
            text-align: center;
            color: #888;
            padding: 30px 0;
        }

        .actions {
            margin-top: 25px;
            text-align: right;
        }

        .write-btn {
            display: inline-block;
            padding: 12px 20px;
            background-color: #ff8c42;
            color: #fff;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 500;
        }

        .write-btn:hover {
            background-color: #ff7a24;
        }
    </style>
</head>
<body>

<div class="container">

    <h1>🐾 게시판</h1>
    <h1>
        {{#username}}
            <p class="username">{{username}} 님, 반갑습니다!</p>
        {{/username}}

    </h1>

    {{#msg}}
        <div class="message">
            {{msg}}
        </div>
    {{/msg}}

    <table>
        <tr>
            <th>ID</th>
            <th>제목</th>
        </tr>

        {{#articleList}}
            <tr>
                <td>{{id}}</td>
                <td>
                    <a href="/articles/{{id}}">
                        {{title}}
                    </a>
                </td>
            </tr>
        {{/articleList}}

        {{^articleList}}
            <tr>
                <td colspan="2" class="empty">
                    아직 게시글이 없습니다 🐶
                </td>
            </tr>
        {{/articleList}}
    </table>

    <div class="actions">
        <a href="/articles/new" class="write-btn">글 작성</a>
    </div>

</div>

</body>
</html>
