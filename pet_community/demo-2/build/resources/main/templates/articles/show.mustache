<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Pet Community | 게시글 상세</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <style>
        body {
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background-color: #f7f8fa;
        }

        .container {
            max-width: 800px;
            margin: 60px auto;
            padding: 0 20px;
        }

        h1 {
            margin-bottom: 24px;
            color: #333;
        }

        table {
            width: 100%;
            background-color: #fff;
            border-collapse: collapse;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
        }

        th, td {
            padding: 16px;
            text-align: left;
            vertical-align: top;
        }

        th {
            width: 120px;
            background-color: #f2f3f5;
            color: #555;
            font-size: 14px;
        }

        td {
            color: #333;
            line-height: 1.6;
        }

        .content {
            min-height: 150px;
            white-space: pre-line;
        }

        .actions {
            margin-top: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .actions-left a {
            color: #666;
            text-decoration: none;
            font-size: 14px;
        }

        .actions-left a:hover {
            text-decoration: underline;
        }

        .actions-right a {
            margin-left: 10px;
            padding: 10px 16px;
            text-decoration: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 500;
        }

        .edit-btn {
            background-color: #ff8c42;
            color: #fff;
        }

        .edit-btn:hover {
            background-color: #ff7a24;
        }

        .delete-btn {
            background-color: #e57373;
            color: #fff;
        }

        .delete-btn:hover {
            background-color: #ef5350;
        }
    </style>
</head>
<body>

<div class="container">

    <h1>📄 게시글 상세</h1>

    <table>
        <tr>
            <th>ID</th>
            <td>{{article.id}}</td>
        </tr>
        <tr>
            <th>제목</th>
            <td>{{article.title}}</td>
        </tr>
        <tr>
            <th>내용</th>
            <td class="content">{{article.content}}</td>
        </tr>
    </table>

    <div class="actions">
        <div class="actions-left">
            <a href="/articles">← 목록으로</a>
        </div>
        <div class="actions-right">
            <a href="/articles/{{article.id}}/edit" class="edit-btn">수정</a>
            <a href="/articles/{{article.id}}/delete" class="delete-btn">삭제</a>
        </div>
    </div>

</div>

</body>
</html>
