<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Pet Community | 로그인</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <style>
        body {
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background-color: #f7f8fa;
        }

        .container {
            max-width: 400px;
            margin: 60px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
        }

        h1 {
            margin-bottom: 20px;
            color: #333;
            text-align: center;
        }

        .message {
            margin-bottom: 20px;
            padding: 12px 16px;
            background-color: #fdecea;
            color: #c62828;
            border-radius: 8px;
            font-weight: 500;
            text-align: center;
        }

        label {
            display: block;
            margin-bottom: 12px;
            font-weight: 500;
            color: #555;
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 10px 12px;
            margin-top: 6px;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-sizing: border-box;
        }

        button {
            display: inline-block;
            width: 100%;
            padding: 12px 20px;
            margin-top: 16px;
            background-color: #ff8c42;
            color: #fff;
            font-weight: 500;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-align: center;
        }

        button:hover {
            background-color: #ff7a24;
        }

        .hi-btn {
            background-color: #6c63ff;
            margin-top: 10px;
        }

        .hi-btn:hover {
            background-color: #574fd6;
        }

        p.username {
            text-align: center;
            font-weight: 500;
            color: #2e7d32;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>

<div class="container">

    <h1>🐾 로그인</h1>

    <!-- 로그인 상태 표시 -->

    <!-- 로그인 실패 메시지 -->
    {{#error}}
        <div class="message">
            로그인 실패: 이메일 또는 비밀번호를 확인하세요.
        </div>
    {{/error}}

    <!-- 로그인 form -->
    <form action="/login" method="post">
        {{#_csrf}}
            <input type="hidden" name="{{parameterName}}" value="{{token}}">
        {{/_csrf}}

        <label>
            Email:
            <input type="text" name="email" placeholder="이메일 입력">
        </label>

        <label>
            Password:
            <input type="password" name="password" placeholder="비밀번호 입력">
        </label>

        <button type="submit">Login</button>
    </form>

    <!-- 별도 버튼: /hi 이동 -->
    <button type="button" class="hi-btn" onclick="location.href='/hi'">메인페이지로 이동</button>

</div>

</body>
</html>
